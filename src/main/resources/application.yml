spring:
  mail:
    host: "smtp.gmail.com"
    port: 587
    username: "shuffle2149@gmail.com"
    password: "frqwptlnpssetliw"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none
    #      use-new-id-generator-mappings: true
    properties:
      hibernate:
        format_sql: true
    generate-ddl: false
#    jdbc:
#      batch_versioned_data: false

  liquibase:
    change-log: "db/changelog/db.changelog-master.xml"
    enabled: true
  datasource:
    url: "jdbc:postgresql://localhost:5432/ttvc"
    #        ?createDatabaseIfNotExist=true
    username: "shuffle"
    password: "shuffle"

storage:
  location: './storage'

server:
  port: 1975

#Для JWT аутентификации:

#  springdoc:
#    show-login-endpoint: false
#    swagger-ui:
#      oauth:
#        client-id: my-client-id
#        client-secret: my-client-secret
#        use-pkce: true

#  настройка контролирует отображение стандартных эндпоинтов аутентификации (например, /login) в Swagger UI документации.
springdoc:
  show-login-endpoint: true