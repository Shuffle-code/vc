spring:
  thymeleaf:
    encoding: UTF-8
    mode: HTML
  logging:
    charset:
      console: UTF-8
      file: UTF-8
  mail:
    host: "smtp.gmail.com"
    port: 587
    username: "shuffle2149@gmail.com"
    password: "frqwptlnpssetliw"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
    generate-ddl: false
  liquibase:
    change-log: "db/changelog/db.changelog-master.xml"
    enabled: true
  datasource:
    url: "jdbc:postgresql://localhost:5439/ttvc"
    username: "shuffle"
    password: "shuffle"


server:
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
  port: 1975
  ssl:
    enabled: true
    key-store-type: "PKCS12"
    key-store: "classpath:keystore.p12"
    key-store-password: "shuffle"
    key-alias: "tomcat"

storage:
  location: './storage'

springdoc:
  show-login-endpoint: true

---
spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: "jdbc:postgresql://localhost:5432/ttvc"
    username: "shuffle"
    password: "rfvby"

storage:
  location: '/home/shuffle/java/storage'

---
spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: "jdbc:postgresql://localhost:5439/ttvc"
    username: "shuffle"
    password: "shuffle"

storage:
  location: './storage'